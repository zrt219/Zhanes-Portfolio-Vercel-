# Vercel Build Doctor Agent Context Snapshot

- Task statement: Build the Vercel Build Doctor Agent as a Vercel-deployable Next.js case-study project, verify it, deploy to Vercel production if credentials allow, and update AI engineering/resume evidence.
- Desired outcome: A polished recruiter-facing app with deterministic log diagnosis, structured APIs, eval harness, security redaction, README, Vercel-ready config, passing test/build, and factual resume/log entry.
- Known facts/evidence: Workspace started as an empty folder with no `package.json` and no `.git`; user supplied full Ralphplan build prompt and AGENTS operating contract.
- Constraints: No hardcoded secrets; no fake production claims; deterministic demo fallback required; use existing requested plugins/skills where available; final report must include verification and log update status.
- Unknowns/open questions: Vercel credentials/project linkage may or may not be configured; GitHub remote is absent locally; production deploy may require external account state.
- Likely codebase touchpoints: `package.json`, Next.js App Router files under `src/app`, deterministic diagnosis utilities under `src/lib`, reusable UI components under `src/components`, tests, README, `.env.example`, `vercel.json`, AI engineering log.
